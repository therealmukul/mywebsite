# Hostinger FTP credentials for `npm run deploy`.
# Copy this file to .env.deploy (which is gitignored) and fill in FTP_PASS.
# Find these under Hostinger hPanel -> Files -> FTP Accounts.

FTP_HOST=82.29.199.235
FTP_USER=u909052788
FTP_PASS=
FTP_PORT=21
# Note: this account's web root is nested under domains/<domain>/ despite the
# hPanel labelling it "public_html". Verify with an FTP client if it changes.
FTP_REMOTE_DIR=domains/mukulsurajiwale.com/public_html
